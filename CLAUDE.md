# CLAUDE.md — 見積アプリ（mitsumori）の決めごと

このファイルはコードから名指しで参照されている。
書き換えるときは、参照している側のコメントも一緒に直すこと。

---

## 芯（これを外したら全部やり直し）

1. **現場の人が、現場で、最初から最後まで作れる**
2. **分からないものは、分からないまま先に進める（止めない）**
3. **人に規則を守らせるのではなく、アプリが例外を覚える**
4. **止まったものが見えること**
5. **迷わせない。** 1つの画面で、押すところを増やさない。
   機能は最大限。でも見た目はシンプルに。
   誰でも使えることを、機能の多さより優先する。
   判断に迷ったら、機能を足すのではなく減らす方を選ぶ。

### 芯が実際にどう効いているか（迷ったときの前例）

- **芯2** — 金額の分からない材料は 0 ではなく `null` で「単価待ち」。
  0は「タダ」の意味になる。null なら1件空けたまま先へ進める
- **芯4** — 単価待ち・未確定・発注待ちは件数で必ず画面に出す。隠さない
- **芯5** — 「出しなおす」は項目が並んだあと出さない（押し間違えると全部消える）。
  項目カードに削除ボタンを置かない。要らない行は時間を0にすれば0円になる
- **芯5** — 実害が無いなら画面を増やさない。
  見えないゴミデータより、押すところが増える方が害が大きい
- **芯5** — 詰める・まとめるのは歓迎（押すところを増やさずに解決している）
- **芯5** — **1枚に収まらないものは、詰めるのではなくページを分ける。**
  「写真から見積」は写真・すること・項目を1枚に載せていて、どれも狭かった。
  2026/8/7 に**写真のページ**と**概算のページ**に分けた。
  行き来はヘッダー右の1つのボタン（`.r-swap`）だけ。**増やすのはそこだけ。**
  ボタンは両方のページで同じ場所に置く。ページごとに動くと探すことになる
- **芯5** — **スクロールで畳む・出す・すべらせるはやらない。**
  2026/8/7 に「写真から見積」でヘッダーと写真の帯をスクロール連動で畳んだが、
  実機で動きが引っかかり、かえって見にくくなって取り消した。
  現場は片手で見るので、画面が動くと目で追えない。
  狭いときは**動きで場所を作らず、高さそのものを削る**。
  写真の欄も下の金額の帯も、いつでも出したままにする
  （[app.css](app.css) `.r-head` / `.r-strip`、[js/screen-rough.js](js/screen-rough.js) `bindScroll`）。
  ただし押すところは40px未満にしない（軍手）。削るのは余白と行送りだけ

---

### 写真の拡大は自前でやる（触る前に読むこと）

`index.html` の viewport は `user-scalable=no` / `maximum-scale=1`、
`app.css` は `* { touch-action: manipulation }`。**ブラウザの拡大は殺してある。**
画面ぜんたいが拡大すると、ヘッダーもボタンも一緒に動いて操作できなくなるため。

だから写真の拡大は [js/photo-viewer.js](js/photo-viewer.js) が `transform` でやる。
拡大するのは写真だけなので、上下のボタンは動かない。
**viewport の設定を緩めて解決しない。** 緩めると全画面が拡大できるようになる。

- `.pv-stage` の `touch-action: none` は必須。無いと iOS が指の動きを先に取る
- `setPointerCapture` は **必ず try で囲む**。投げると pointerdown の残りが死んで、
  拡大も送りも黙って効かなくなる（2026/8/7 に踏んだ）
- 上限6倍。写真は送るときに長辺2000pxへ縮めてある（`shrinkImage`）ので、
  390pxの画面なら約5倍で元の粒に届く。それ以上はぼやけるだけ
- ビューアは Firebase を知らない。消すのは呼ぶ側に任せてある。
  そのおかげで [tools/test-photo-viewer.html](tools/test-photo-viewer.html) から
  **実物のまま**指の動きを試せる。**写しを作らないこと**（写しだけ直る）

---

## 社長への確認事項（第3部）

| # | 内容 | 状態 | いつまでに |
|---|---|---|---|
| 1 | データが外部に送られることの了解 | **済** 2026/8/6 社長 | — |
| 2 | APIキーを誰が取るか | **済** 担当k | — |
| 3 | 職種と単価は今も合っているか | 未確認 | **現場に配る前** |
| 4 | 法定福利費を分けて書く件（元請けの書式指定） | 未確認 | **現場に配る前** |
| 5 | 過去の実績を10〜20件思い出せるか | 未確認 | 相場がよつばの金額になるまでに |

コード側では [js/rough-store.js](js/rough-store.js)（第3部 4 = 法定福利費）と
[js/actuals.js](js/actuals.js)（第3部 5 = 過去の実績）がこの番号で書かれている。
※**番号は決めごとの参照に使わない。** 振り直すたびにズレる。中身で書くこと。

### 3・4 が現場に配る前に要る理由

- **3 職種と単価** … 合っていないと、AIが出した人工に間違った単価が掛かる。
  ひな形から出しても同じ。AIかどうかに関係なく、金額そのものが狂う
- **4 法定福利費** … 元請けへの提出書式そのもの。
  `separateWelfare` の既定は `false`。元請けへの確認が済むまで誰も `true` にしない
  （[js/rough-store.js](js/rough-store.js) `setSeparateWelfare`）。
  これは「書き方」の指定で、計算は変わらない。金額には一切効かせていない

---

## AI を入れる条件

> **「APIキーの取得」と「外部送信の了解」が未了のうちは、**
> **Functions と AI 呼び出しを実装しない**

※ここは番号で書かない。**中身で書く。**
　以前は「3と4が未了のうちは」と番号で書いてあったが、確認事項の番号を
　振り直したときにどれを指すのか分からなくなった。番号は動く。中身は動かない。

| 条件 | 状態 |
|---|---|
| APIキーの取得 | **済** 担当k |
| 外部送信の了解 | **済** 2026/8/6 社長 |

どちらも済んだので実装した。
使いはじめた日から記録が残らないと、半年後に実績が0件になる。だから保存が先だった。

現状: **入っている**（2026/8/7）

- 受付 [functions/index.js](functions/index.js) … 実装済・デプロイ済
- `isAiAvailable()`（[js/rough-generate.js](js/rough-generate.js)）… `true`

**AIのスイッチはこの1行だけ。** 止めたいときはここを `false` に戻して push する。

### 入口の守り（触る前に読むこと）

受付は静的サイトから呼ぶので、入口は**誰からでも届く場所**に置くしかない。
そのままでは、公開されている apiKey を見た誰でも通れてAIの料金を焼かれる。
だから**締めてから開けた**。この順番を逆にしないこと。

| 守り | 中身 |
|---|---|
| App Check | reCAPTCHA Enterprise。[js/firebase.js](js/firebase.js) で初期化、受付側は `enforceAppCheck: true` |
| Anthropic の上限 | 月 $10・通知 $5（抜けられても、ここで止まる） |

- **App Check は受付にだけ効かせている。** Firestore と Storage には強制しない。
  そこまで締めると、電波の悪い現場で下書きの保存まで巻き添えで止まる（芯2）
- ドメインは `nomurakougyouipad-oss.github.io` で登録済み。**別のドメインで開くと通らない**
- 組織ポリシー「共有先のドメインを制限」を `mitsumori-35e9d` だけ外してある。
  外さないと Cloud Run が門前払いして、受付までそもそも届かない

### 受付を直したら push だけでは反映されない

`functions/index.js` は Google 側にある。GitHub Pages が配るのはアプリ側だけ。
`firebase deploy --only functions:estimateFromPhotos --project mitsumori-35e9d` が要る。
手順とつまずきどころは [functions/デプロイ手順.md](functions/デプロイ手順.md)。
**この端末には firebase CLI も gcloud も入っていない。** 上げるのは Node のある端末。

### よつばの単価と相場は2つで1組。片方だけ強めない（2026/8/8）

**片方を強めるときは、もう片方も同じ強さで書く。** これが結論。

8/7 に相場を必ず入れさせる直しをしたとき、同じコミットで
`SYSTEM` の禁止リストに **`trade・persons・hours` を名指しで足してしまった**。
よつばの単価はこの3つから出る（[js/rough-calc.js](js/rough-calc.js) `yotsubaBase`）。
結果、相場は出るようになったが**今度はよつばの単価が消えた**
（配管工事／「ステンレス・40A配管・60m」で再現）。

**人工の見立て（trade・persons・hours）は推測ではない。**
アプリが毎回聞いている①②そのもので、写真から読み取る事実ではない。
禁止してよいのは、写真にも一言にも書いていない **数量（qty）と よつばの仕入れ値（cost）だけ**。

- `cost` はAIには分からない（単価マスター1,567品目の数字）。**必ず null**。
  受付側でも `cost: null` で上書きして二重に守っている。
  作り話の数字が「よつばの単価」の顔をして合計候補に出るのがいちばん悪い
- そのぶん**材料・外注のよつば側は空く**。それは単価待ちで正しい（芯2）。
  相場は入るので比べる材料は残る。**両方そろって当たり前なのは労務と移動**

#### 職種名はアプリから渡す

よつばの単価は「職種名 → 円/工数」の表を引く。AIが表に無い名前（例「配管工」）を返すと
その場で単価が引けず、**よつばの単価が黙って消える**。だから聞くときに一覧を渡す。
渡すのは**名前だけ**。社内の単価そのものは外へ出さない。

**受付が自分で Firestore を読む形にはしない。** 一度そう書いて上げたら
`PERMISSION_DENIED`（受付のサービスアカウントに Firestore の読み取り権限が無い）。
権限を足すより、表を持っているアプリから渡すほうが早くて確か。
古いアプリから呼ばれたときのために、受付側に既定の4つを残してある。

#### 片方だけを黙らせない

聞き方をどれだけ強く書いても、返さない回は出る。文章だけに頼らない。

1. 返ってきたものを受付が数える（`coverageOf`）
2. 欠けていたら**1回だけ聞き直す**（`fillGaps`。写真は送らないので安い）。
   埋めるのは空いている欄だけ。入っている値は上書きしない
3. それでも残ったぶんは件数で返し、**画面の帯とトーストに出す**（芯4）

確かめるのは [tools/test-ai.html](tools/test-ai.html)。**②相場と③よつばの単価を必ず並べて出す。**
片方だけ見て直すと、もう片方が落ちても気づけない。それを2回やった。
文言は画面の欄で変えられる。症状が出た文言そのままで試すこと。

### 相場（marketAmount）に「分からなければ null」と書かない

2026/8/7、概算に「世の中の相場」が1件も出なかった。
調べたら **受付・App Check・画面はすべて正常**で、AIも動いていた
（直近4件のうち3件が `source:'ai'`・全項目 `未確定`）。
**AIが `marketAmount` を返したり返さなかったりしていた**（20項目中11件の回と、16項目全部 null の回）。

原因は聞き方。`SYSTEM` の「写真から読み取れないこと（寸法・型式・kW・数量）を
推測で埋めないでください」が**相場にも効いていた**。schema の説明も
「分からなければ null」で、null が楽な逃げ道になっていた。

**寸法・型式・kW・数量は推測してはいけない。相場はもともと当てにいく数字で性質が逆。**
禁止の対象は `qty・cost・trade・persons・hours` に限る。
`marketAmount` は「必ず入れる。null は例外」と書く。**書き戻さないこと。**

切り分けは [tools/test-ai.html](tools/test-ai.html)。①受付に届くか ②相場を返しているか
③写真が読めているか を順に出す。**実際にAIを呼ぶので1回数十円かかる。**

### 「動くはず」で true にしない

デプロイが成功しただけでは動かない。実際に外から呼んで、
**受付自身がJSONを返すところまで**見てから `true` にすること。
（デプロイ成功と、アプリから呼べることは別。一度これで踏みかけた）

---

## AIの使い分け（実測で決めた）

| やること | 使うもの |
|---|---|
| 図面・写真を読む | **Claude** |
| 作業項目を並べる | **ChatGPT** |
| スケッチを描く | **ChatGPT** |
| 人工・相場 | どちらでも |

**読むのと描くのは別の能力なので分ける。**

寸法・数量・kW・型式だけ2社で答え合わせする。
一致 → 自動で入れる／食い違い → 「ききたいこと」に回す。
**一致しても元の写真は必ず画面に残す**（2社が同じ間違いをすることがある）。

### いまの実装との差

受付 [functions/index.js](functions/index.js) は **Claude 1社で「読む」と「並べる」の両方**をやっている。
上の表では「並べる」は ChatGPT。答え合わせも入っていない。
Anthropic のキーしか無い時点で作ったための簡略版。ChatGPT のキーが取れたら、
`estimateFromPhotos` を2社に分けて答え合わせを入れる。

---

## 金額の決めごと

### AIの金額はそのまま合計に入れない

AIが出した項目は必ず `state:'未確定'` で入る。
人が「この金額を使う／金額を直す／単価待ちにする」のどれかを押すまで、合計に入らない。
受付側でも `state` を上書きして二重に守っている（[functions/index.js](functions/index.js)）。

分からない金額は **0 ではなく null**（芯2）。

### 工数で数える（2026/8/7）

**1工数 ＝ 8時間**（1人が1日）。刻みは **0.5工数（＝4時間）**。
`36h` とは出さない。`4.5工数` と出す。

> 36h × 時間単価 ＝ 4.5工数 × 工数単価　（工数単価 ＝ 時間単価 × 8）

**数え方を変えただけ。金額の出し方は何も変えていない。**

| | 単位 |
|---|---|
| 労務 | **工数**（0.5刻み） |
| 移動 | **時間のまま**。片道1時間を0.125工数と書いても読めない |
| 手順（くわしくの中） | **時間のまま**。門型2h・玉掛け1h は工数だと刻めない |

#### 保存の形は変えていない（ここが肝）

- `item.hours` / `step.hours` は **今までどおり「時間」**。見せるときに8で割る
- 単価は `trades = [{name, kosuRate, rate}]`
  - `kosuRate` … 円/工数。**これからはこちらが正**
  - `rate` … 円/h。前からある欄。**消さない**。`kosuRate` が無いときだけ使う
  - 単価を直したときは **両方書く**（[js/rough-calc.js](js/rough-calc.js) `tradeWithKosuRate`）

**古い欄を消さない理由。** 単価は保存済みの見積すべてに焼き付けてある（`unitRatesFrozen`）。
会社の標準・元請けごと・この見積だけ・項目ごと・手順ごとの**5か所**に散っている。
欄の意味をまとめて8倍に読み替えると、**1か所でも漏れたその見積の金額が8倍ずれる。**
「保存したら金額は動かない」を守るため、古い欄は古い意味のまま残した。
→ `kosuRate` が無いデータでは `tradeRate` が前と1円も変わらない値を返す。

確認は [tools/test-kosu.html](tools/test-kosu.html)。ひな形「機械の修理・整備」＝
ポンプ整備の並びで、項目ごと・費目ごと・合計・提出価格の幅まで突き合わせる。
**単価の持ち方を触ったら必ずこれを通すこと。**

### 一覧は見るところ、くわしくは直すところ（2026/8/7）

概算の項目一覧からは**直す口を全部外した**。±の足し引き、人数・時間・km の数字、
金額を直すボタン。カードぜんたいが「くわしく」への入口で、**押すところは1枚に1つ**。

直すのも消すのも [js/screen-item.js](js/screen-item.js) の中だけ。費目に関係なく
どの項目からも開ける（材料・外注・単価待ち・未確定も）。**一覧に直すボタンを戻さないこと。**

例外は未確定の2つ（この金額を使う／単価待ちにする）。
これは金額を直すのではなく、**合計に入れるかどうかを決める**ボタン。

### 率と単価の3段階

> 会社の標準 → 元請けごと → この見積だけ、の順に上書き。
> 書いていないところは上の段をそのまま使う。
> — [js/rough-calc.js](js/rough-calc.js) `resolveRates` / `resolveUnitRates`

### 保存したら金額は動かない

保存のときに率と単価を焼き付ける（`freezeRough`）。
あとで設定を変えても、出した見積の金額は変わらない。

### 計算の正は Excel内訳書と calc.js

諸経費・損料 ＝ 材料＋労務／法定福利費 ＝ 労務のみ。移動費・外注費は入らない。
AIにもアプリの画面にも計算はさせない。金額は必ず [js/rough-calc.js](js/rough-calc.js) で出す。

---

## 上書きしない

> 概算・本見積・実績はそれぞれ別に残す。
> 概算は `roughEstimates`、本見積は `estimates`。概算から本見積を作っても概算は消えない。
> — [js/rough-store.js](js/rough-store.js)

一覧には3つとも並ぶ。だから「あとで見返して、見立てが当たっていたか」が分かる。

---

## 率・単価の変更履歴

> 承認は挟まない。全員が変えられる。ただし履歴には必ず残る。
> — [js/rate-history.js](js/rate-history.js)

画面「設定 — 率と労務単価」はこの1行をそのまま出す:

```
2026/8/6  東レ愛媛  現場工事 4,000→3,800  野村
          「今年度から単価が下がった」
```

承認を挟まないのは芯3。人に規則を守らせるのではなく、
変えたことをアプリが覚えて、あとから理由まで辿れるようにする。

---

## 配り方

- 静的サイト。GitHub Pages がリポジトリを**丸ごと**公開する（`path: .`）
  → **秘密をリポジトリに置かない。** APIキーは Google Secret Manager
  → `functions/node_modules/` は `.gitignore` で必ず除く
- 直したら `sw.js` の `VERSION` を上げる。上げないと iPad は古いまま
- ローカル確認だけで「実装しました」と言わない。push して公開に反映されたのを見てから
