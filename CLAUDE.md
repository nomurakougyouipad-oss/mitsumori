# CLAUDE.md — 見積アプリ（mitsumori）の決めごと

このファイルはコードから名指しで参照されている。
書き換えるときは、参照している側のコメントも一緒に直すこと。

※このファイルは 2026/8/7 に作り直したもの。それまで実体が無く、
　コードのコメントだけが「CLAUDE.md にこう書いてある」と参照している状態だった。
　中身は**コード中に引用されている文言と、実際に決まった事実だけ**で組み直してある。
　下の「まだ埋まっていないところ」は、覚えている人が足すこと。

---

## AI を入れる前の4条件

> 「3と4が未了のうちは、Functions と AI 呼び出しを実装しない」
> — 参照: [js/rough-generate.js](js/rough-generate.js) / [js/rough-store.js](js/rough-store.js)

使いはじめた日から記録が残らないと、半年後に実績が0件になる。だから保存が先。

| | 条件 | 状態 |
|---|---|---|
| 1 | （まだ埋まっていない） | — |
| 2 | （まだ埋まっていない） | — |
| 3 | APIキーを誰が取るか | **済** 2026/8/7 |
| 4 | データが外部に送られることの了解 | **済** 2026/8/6 社長 |

3と4が済んだので、受付（[functions/index.js](functions/index.js)）を実装した。

**AIのスイッチは [js/rough-generate.js](js/rough-generate.js) の `isAiAvailable()` 1行。**
`firebase deploy --only functions` が済んでから `true` にする。
デプロイ前に `true` にすると、現場が「項目を出す」を押した瞬間に止まる。

---

## 金額の決めごと

### AIの金額はそのまま合計に入れない

AIが出した項目は必ず `state:'未確定'` で入る。
人が「この金額を使う／金額を直す／単価待ちにする」のどれかを押すまで、合計に入らない。
受付側でも `state` を上書きして二重に守っている（[functions/index.js](functions/index.js)）。

分からない金額は **0 ではなく null**。0は「タダ」の意味になってしまう。
null なら「単価待ち」として空けたまま先へ進める。

### 率と単価の3段階

> 会社の標準 → 元請けごと → この見積だけ、の順に上書き。
> 書いていないところは上の段をそのまま使う。
> — 参照: [js/rough-calc.js](js/rough-calc.js) `resolveRates` / `resolveUnitRates`

### 保存したら金額は動かない

保存のときに率と単価を焼き付ける（`freezeRough`）。
あとで設定を変えても、出した見積の金額は変わらない。

### 計算の正は Excel内訳書と calc.js

諸経費・損料 ＝ 材料＋労務／法定福利費 ＝ 労務のみ。移動費・外注費は入らない。
AIにもアプリの画面にも計算はさせない。金額は必ず [js/rough-calc.js](js/rough-calc.js) で出す。

---

## 上書きしない

> 概算・本見積・実績はそれぞれ別に残す。
> 概算は `roughEstimates`、本見積は `estimates`。概算から本見積を作っても概算は消えない。
> — 参照: [js/rough-store.js](js/rough-store.js)

一覧には3つとも並ぶ。だから「あとで見返して、見立てが当たっていたか」が分かる。

**AIが一致を出しても元の写真は必ず残す**（[js/screen-rough.js](js/screen-rough.js)）。
2社が同じ間違いをすることがある。消すのは人が押したときだけ。

---

## 率・単価の変更履歴

> 承認は挟まない。全員が変えられる。ただし履歴には必ず残る。
> — 参照: [js/rate-history.js](js/rate-history.js)

画面「設定 — 率と労務単価」はこの1行をそのまま出す:

```
2026/8/6  東レ愛媛  現場工事 4,000→3,800  野村
          「今年度から単価が下がった」
```

---

## 芯5 — 迷わせない

使うのは現場の職人。押すところが1つ増えるたびに迷う。

- **押すところを増やさない。** モックにあるものだけ置く
- **画面を増やさない。** 実害が無いなら、見えないゴミデータより画面が増える方が害が大きい
- 詰める・畳む・まとめるのは歓迎（増やさずに解決している）
- 危ないボタンを出さない。「出しなおす」のように押し間違えると全部消えるものは置かない

※芯1〜4もあるはずだが、コード中に引用が無いので書けていない。覚えている人が足すこと。

---

## 配り方

- 静的サイト。GitHub Pages がリポジトリを**丸ごと**公開する（`path: .`）
  → **秘密をリポジトリに置かない。** APIキーは Google Secret Manager
  → `functions/node_modules/` は `.gitignore` で必ず除く
- 直したら `sw.js` の `VERSION` を上げる。上げないと iPad は古いまま
- ローカル確認だけで「実装しました」と言わない。push して公開に反映されたのを見てから
